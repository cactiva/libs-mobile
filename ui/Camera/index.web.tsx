import { observer } from "mobx-react-lite";
import React from "react";
import { Text } from "react-native";

export default observer((props: any) => {
  return <Text>Camera not support web.</Text>;
});
